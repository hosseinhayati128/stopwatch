<Window x:Class="StopwatchOverlay.NoteEntryWindow" Style="{StaticResource {x:Type Window}}"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:ac="clr-namespace:StopwatchOverlay.Themes" Title="Note Entry" ac:AcanthusVisual.Scope="{DynamicResource OrnamentVisibility}"
 WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="False"
 ResizeMode="CanResizeWithGrip" Width="520" Height="250" MinWidth="360" MinHeight="250"
 WindowStartupLocation="CenterScreen" Loaded="Window_Loaded" PreviewKeyDown="Window_PreviewKeyDown">
 <Window.Resources><ResourceDictionary><ResourceDictionary.MergedDictionaries>
  <ResourceDictionary Source="Themes/PirateNotesStyles.xaml"/><ResourceDictionary Source="Themes/Overlay/OverlayStyles.xaml"/>
 </ResourceDictionary.MergedDictionaries>
 <Style x:Key="NoteTextBoxStyle" TargetType="TextBox">
  <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/><Setter Property="CaretBrush" Value="{DynamicResource AccentBrush}"/>
  <Setter Property="Background" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="2"/>
  <Setter Property="FontSize" Value="14"/><Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}"/>
  <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TextBox">
   <Grid Background="{TemplateBinding Background}">
    <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"/>
    <TextBlock x:Name="PlaceholderText" Text="{TemplateBinding Tag}" Foreground="{TemplateBinding Foreground}"
      FontSize="{TemplateBinding FontSize}" FontFamily="{TemplateBinding FontFamily}" FontWeight="{TemplateBinding FontWeight}"
      Padding="{TemplateBinding Padding}" TextWrapping="Wrap" IsHitTestVisible="False" Visibility="Collapsed"/>
   </Grid>
   <ControlTemplate.Triggers><Trigger Property="Text" Value=""><Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/></Trigger></ControlTemplate.Triggers>
  </ControlTemplate></Setter.Value></Setter>
  <Style.Triggers><Trigger Property="ac:PirateVisual.Enabled" Value="True">
   <Setter Property="Foreground" Value="#512B16"/><Setter Property="CaretBrush" Value="#317E70"/>
   <Setter Property="FontFamily" Value="{StaticResource NotesBodyFont}"/><Setter Property="FontSize" Value="25"/>
  </Trigger></Style.Triggers>
 </Style>
 <Style x:Key="EntryHeadingStyle" TargetType="TextBlock" BasedOn="{StaticResource NotesHeadingStyle}">
  <Style.Triggers><Trigger Property="ac:PirateVisual.Enabled" Value="True">
   <Setter Property="FontSize" Value="42"/><Setter Property="HorizontalAlignment" Value="Center"/>
  </Trigger></Style.Triggers>
 </Style>
 </ResourceDictionary></Window.Resources>
 <ac:NoteArtworkBorder x:Name="NoteFrame" Artwork="Frame" Style="{StaticResource NotesFrameStyle}" Margin="8" LayoutTransform="{DynamicResource ApplicationScaleTransform}" SizeChanged="NoteFrame_SizeChanged">
  <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <Grid x:Name="EntryHeader" MouseLeftButtonDown="Header_MouseLeftButtonDown">
    <Grid.Style><Style TargetType="Grid"><Setter Property="Margin" Value="0,0,0,12"/>
     <Style.Triggers><Trigger Property="ac:PirateVisual.Enabled" Value="True"><Setter Property="Margin" Value="0,36,0,14"/></Trigger></Style.Triggers>
    </Style></Grid.Style>
    <TextBlock x:Name="BadgeIcon" Text="✦" FontSize="15" HorizontalAlignment="Left" VerticalAlignment="Center">
     <TextBlock.Style><Style TargetType="TextBlock"><Style.Triggers><Trigger Property="ac:PirateVisual.Enabled" Value="True"><Setter Property="Visibility" Value="Collapsed"/></Trigger></Style.Triggers></Style></TextBlock.Style>
    </TextBlock>
    <Viewbox Margin="26,0" Stretch="Uniform" StretchDirection="DownOnly" VerticalAlignment="Center">
     <TextBlock x:Name="HeadingText" Text="Add Note" Style="{StaticResource EntryHeadingStyle}" TextWrapping="NoWrap"/>
    </Viewbox>
    <Button x:Name="CloseButton" HorizontalAlignment="Right" VerticalAlignment="Top" Width="22" Height="22" Style="{StaticResource OverlayCloseButtonStyle}" ToolTip="Close (Esc)" Click="CloseButton_Click">
     <Path Width="9" Height="9" Data="M1,1L8,8 M8,1L1,8" Stroke="{Binding Foreground,RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.5"/>
    </Button>
   </Grid>
   <ac:NoteArtworkBorder x:Name="PaperSurface" Grid.Row="1" Artwork="Paper" Style="{StaticResource NotesPaperStyle}" MinHeight="70">
    <TextBox x:Name="NoteInputBox" Style="{StaticResource NoteTextBoxStyle}" AcceptsReturn="True" TextWrapping="Wrap"
       VerticalScrollBarVisibility="Auto" PreviewKeyDown="NoteInputBox_PreviewKeyDown" AutomationProperties.Name="Note text"/>
   </ac:NoteArtworkBorder>
   <Grid Grid.Row="2" Margin="0,12,0,0"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="14"/><ColumnDefinition/></Grid.ColumnDefinitions>
     <Button x:Name="CancelButton" Content="Cancel" Click="CloseButton_Click" Style="{StaticResource NotesCompassButtonStyle}" ToolTip="Cancel (Esc)"/>
     <Button x:Name="SaveButton" Grid.Column="2" Content="Save" Click="SaveButton_Click" Style="{StaticResource NotesCompassButtonStyle}" ToolTip="Save (Ctrl+Enter)"/>
    </Grid>
    <TextBlock Grid.Row="1" Text="Ctrl+Enter to Save  ·  Esc to Cancel" HorizontalAlignment="Center" FontSize="10" Margin="0,7,0,0">
     <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/><Style.Triggers>
      <Trigger Property="ac:PirateVisual.Enabled" Value="True"><Setter Property="Foreground" Value="#DFC292"/></Trigger>
     </Style.Triggers></Style></TextBlock.Style>
    </TextBlock>
   </Grid>
  </Grid>
 </ac:NoteArtworkBorder>
</Window>
