<Window x:Class="StopwatchOverlay.ShortcutCommandHintWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Timer Command Mode"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowActivated="False"
        Focusable="False"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Loaded="Window_Loaded">
    <Border Background="{DynamicResource SurfaceRaisedBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="{DynamicResource ThemePanelBorderThickness}"
            CornerRadius="{DynamicResource ThemePanelCornerRadius}"
            Padding="16,10"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="14" Direction="270" ShadowDepth="3" Opacity="0.55"/>
        </Border.Effect>
        <StackPanel HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,4">
                <TextBlock Text="✦ " Foreground="{DynamicResource AccentBrush}" FontSize="12" FontWeight="Bold"/>
                <TextBlock Text="Timer Commands:"
                           Foreground="{DynamicResource AccentBrush}"
                           FontSize="12"
                           FontFamily="{DynamicResource AppFontFamily}"
                           FontWeight="SemiBold"/>
            </StackPanel>
            <TextBlock Text="Space Start/Stop · R Reset · O Overlay · L Lap · W Controller"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       FontSize="12"
                       FontFamily="{DynamicResource AppFontFamily}"
                       FontWeight="Medium"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="C Clock · N New · T Next · X Close · P Project · D Dashboard · E Edit · A Record · S Sync"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       FontSize="12"
                       FontFamily="{DynamicResource AppFontFamily}"
                       FontWeight="Medium"
                       HorizontalAlignment="Center"
                       Margin="0,3,0,0"/>
            <TextBlock Text="Press key to select · Esc to cancel"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       FontSize="11"
                       FontFamily="{DynamicResource AppFontFamily}"
                       HorizontalAlignment="Center"
                       Margin="0,3,0,0"/>
        </StackPanel>
    </Border>
</Window>
