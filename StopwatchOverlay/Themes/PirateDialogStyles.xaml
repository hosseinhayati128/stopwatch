<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:p="clr-namespace:StopwatchOverlay.Themes">
    <Style x:Key="PirateDialogScroll" TargetType="ScrollViewer">
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="CanContentScroll" Value="False"/>
        <Style.Triggers>
            <Trigger Property="p:PirateVisual.Enabled" Value="True">
                <Setter Property="Background" Value="{DynamicResource PirateWood}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="PirateDialogSurface" TargetType="Border" BasedOn="{StaticResource DialogSurface}">
        <Style.Triggers>
            <Trigger Property="p:PirateVisual.Enabled" Value="True">
                <Setter Property="Background" Value="{DynamicResource PiratePaperBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource PirateBrass}"/>
                <Setter Property="BorderThickness" Value="3"/>
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="TextElement.Foreground" Value="#3C1C11"/>
                <Setter Property="Effect"><Setter.Value><DropShadowEffect Color="#170C07" BlurRadius="8" ShadowDepth="3" Opacity="0.75"/></Setter.Value></Setter>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="PirateDialogSkull" TargetType="Rectangle">
        <Setter Property="Visibility" Value="Collapsed"/>
        <Setter Property="Fill" Value="{DynamicResource PirateSkull}"/>
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
        <Style.Triggers>
            <Trigger Property="p:PirateVisual.Enabled" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
